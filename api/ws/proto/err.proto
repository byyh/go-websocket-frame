syntax = "proto3";

package social.api.ws;


enum CmdErr {
	RetErr = 0;
}

message ErrResp {
	// 错误编码
    int32 code = 1;
    // 错误信息
    string msg = 2;
    // cmd
    uint32 origin_cmd = 3;
}

